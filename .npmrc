# NPM Configuration for Vercel Deployment
# This file tells npm to use --legacy-peer-deps automatically

# Allow legacy peer dependencies (required for Next.js 16 + x402-next compatibility)
legacy-peer-deps=true

# Optional: Increase timeout for slow networks
fetch-timeout=600000

# Optional: Disable strict SSL (only if needed)
# strict-ssl=false

